---
import '../styles/global.css';
import Code from '../components/Code.astro';
import Footer from '../components/Footer.astro';
---

<html lang="en" data-theme="dark">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Setup - 2026 Tech Stack</title>
		<script is:inline>
			(function() {
				const savedTheme = localStorage.getItem('theme') || 'dark';
				document.documentElement.setAttribute('data-theme', savedTheme);
			})();
		</script>
	</head>
	<body class="body-base">
		<nav class="navbar">
			<div class="navbar-container">
				<div class="navbar-content">
					<a href="/" class="navbar-title">
						2026 Tech Stack
					</a>
					<nav class="navbar-nav">
						<a href="/" class="navbar-nav-link">Home</a>
						<a href="/setup" class="navbar-nav-link">Setup</a>
					</nav>
					<div class="navbar-button-group">
						<button
							id="theme-toggle"
							class="theme-toggle"
							aria-label="Toggle theme"
							type="button"
						>
							<svg class="sun-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
								<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
							</svg>
							<svg class="moon-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
								<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
							</svg>
						</button>
						<a
							href="https://github.com/Ma77h3hac83r/2026TechStack"
							target="_blank"
							rel="noopener noreferrer"
							class="navbar-button navbar-button-icon"
							aria-label="GitHub"
						>
							<svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
								<path fill-rule="evenodd" d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z" clip-rule="evenodd" />
							</svg>
						</a>
						<a
							href="https://x.com/The_Ma77rix"
							target="_blank"
							rel="noopener noreferrer"
							class="navbar-button navbar-button-icon"
						>
							<img src="/x_light.svg" alt="X/Twitter" class="icon-dark" />
							<img src="/x_dark.svg" alt="X/Twitter" class="icon-light" />
						</a>
					</div>
				</div>
			</div>
		</nav>
		<main class="container-main">
			<section class="section">
				<h2 class="section-title">Project Setup Guide</h2>
				<div class="tab-nav">
					<button
						data-tab="astro"
						class="tab-button active"
					>
						Astro
					</button>
					<button
						data-tab="tailwind"
						class="tab-button"
					>
						Tailwind
					</button>
					<button
						data-tab="biome"
						class="tab-button"
					>
						Biome
					</button>
					<button
						data-tab="firebase"
						class="tab-button"
					>
						Firebase
					</button>
					<button
						data-tab="github"
						class="tab-button"
					>
						GitHub
					</button>
					<button
						data-tab="stripe"
						class="tab-button"
					>
						Stripe
					</button>
					<button
						data-tab="x402"
						class="tab-button"
					>
						x402
					</button>
				</div>
				<div class="tab-content-wrapper">
					<div id="tab-astro" class="tab-content active">
						<h3 class="heading-2">
							<span class="text-blue">Astro Setup</span>
						</h3>
						<h4 class="heading-3">1. Create a new Astro project.</h4>
						<Code code={`$ npm create astro@latest project-name
$ mv ./project-name/* . # Moves project to root folder`} lang="bash" />
					</div>
					<div id="tab-tailwind" class="tab-content hidden">
						<h3 class="heading-2">
							<span class="text-blue">Add Tailwind CSS</span>
						</h3>
						<h4 class="heading-3">1. Install Tailwind CSS.</h4>
						<Code code={`$ npm install tailwindcss @tailwindcss/vite`} lang="bash" />
						<h4 class="heading-3">2. Add Tailwind CSS to your Astro project.</h4>
						<p class="text-body">Add to <code class="code-inline">astro.config.mjs</code>:</p>
						<Code code={`import tailwindcss from "@tailwindcss/vite";

vite {
  plugins: [tailwindcss()],
},`} lang="javascript" />
						<p class="text-body">Create <code class="code-inline">/src/styles/global.css</code> and add:</p>
						<Code code={`@import "tailwindcss";`} lang="css" />
					</div>
					<div id="tab-biome" class="tab-content hidden">
						<h3 class="heading-2">
							<span class="text-blue">Biome Setup</span>
						</h3>
						<h4 class="heading-3">1. Install Biome and initialize it.</h4>
						<Code code={`$ npm i -D -E @biomejs/biome
$ npx @biomejs/biome init`} lang="bash" />
					</div>
					<div id="tab-firebase" class="tab-content hidden">
						<h3 class="heading-2">
							<span class="text-blue">Firebase Setup</span>
						</h3>
						<h4 class="heading-3">1. Create a new Firebase project at <a href="https://console.firebase.google.com/" target="_blank" rel="noopener noreferrer" class="link-external">Firebase Console</a></h4>
						<h4 class="heading-3">2. Install Firebase packages.</h4>
						<Code code={`$ npm install firebase firebase-admin`} lang="bash" />
						<h4 class="heading-3">3. Create client and server Firebase files.</h4>
						<p class="text-body">Create <code class="code-inline">/src/firebase/client.ts</code>:</p>
						<Code code={`import { initializeApp } from "firebase/app";

const firebaseConfig = {
  apiKey: import.meta.env.PUBLIC_FIREBASE_API_KEY,
  authDomain: import.meta.env.PUBLIC_FIREBASE_AUTH_DOMAIN,
  projectId: import.meta.env.PUBLIC_FIREBASE_PROJECT_ID,
  storageBucket: import.meta.env.PUBLIC_FIREBASE_STORAGE_BUCKET,
  messagingSenderId: import.meta.env.PUBLIC_FIREBASE_MESSAGING_SENDER_ID,
  appId: import.meta.env.PUBLIC_FIREBASE_APP_ID,
};

export const app = initializeApp(firebaseConfig);`} lang="typescript" />
						<p class="text-body">Create <code class="code-inline">/src/firebase/server.ts</code> for server-side admin SDK:</p>
						<Code code={`import type { ServiceAccount } from "firebase-admin";
import { initializeApp, cert, getApps } from "firebase-admin/app";

const activeApps = getApps();
const serviceAccount = {
  type: "service_account",
  project_id: import.meta.env.FIREBASE_PROJECT_ID,
  private_key_id: import.meta.env.FIREBASE_PRIVATE_KEY_ID,
  private_key: import.meta.env.FIREBASE_PRIVATE_KEY,
  client_email: import.meta.env.FIREBASE_CLIENT_EMAIL,
  client_id: import.meta.env.FIREBASE_CLIENT_ID,
  auth_uri: import.meta.env.FIREBASE_AUTH_URI,
  token_uri: import.meta.env.FIREBASE_TOKEN_URI,
  auth_provider_x509_cert_url: import.meta.env.FIREBASE_AUTH_CERT_URL,
  client_x509_cert_url: import.meta.env.FIREBASE_CLIENT_CERT_URL,
};

const initApp = () => {
  if (import.meta.env.PROD) {
    console.info('PROD env detected. Using default service account.')
    return initializeApp()
  }
  console.info('Loading service account from env.')
  return initializeApp({
    credential: cert(serviceAccount as ServiceAccount)
  })
}

export const app = activeApps.length === 0 ? initApp() : activeApps[0];`} lang="typescript" />
						<h4 class="heading-3">4. Add environment variables to <code class="code-inline">.env</code>.</h4>
						<Code code={`interface ImportMetaEnv {

  readonly PUBLIC_FIREBASE_API_KEY: string;
  readonly PUBLIC_FIREBASE_AUTH_DOMAIN: string;
  readonly PUBLIC_FIREBASE_PROJECT_ID: string;
  readonly PUBLIC_FIREBASE_STORAGE_BUCKET: string;
  readonly PUBLIC_FIREBASE_MESSAGING_SENDER_ID: string;
  readonly PUBLIC_FIREBASE_APP_ID: string;

  readonly FIREBASE_PROJECT_ID: string;
  readonly FIREBASE_PRIVATE_KEY_ID: string;
  readonly FIREBASE_PRIVATE_KEY: string;
  readonly FIREBASE_CLIENT_EMAIL: string;
  readonly FIREBASE_CLIENT_ID: string;
  readonly FIREBASE_AUTH_URI: string;
  readonly FIREBASE_TOKEN_URI: string;
  readonly FIREBASE_AUTH_CERT_URL: string;
  readonly FIREBASE_CLIENT_CERT_URL: string;
}

interface ImportMeta {
  readonly env: ImportMetaEnv;
}`} lang="env" />
					</div>
					<div id="tab-github" class="tab-content hidden">
						<h3 class="heading-2">
							<span class="text-blue">GitHub Pages & Actions</span>
						</h3>
						<div class="mb-6">
							<h4 class="heading-3">1. Enable GitHub Pages</h4>
							<p class="text-body">
								On GitHub, go to your repository <strong>Settings</strong> → <strong>Pages</strong> → Under <strong>Sources</strong>, select <strong>GitHub Actions</strong>.
							</p>
						</div>
						<div class="mb-6">
							<h4 class="heading-3">2. Update astro.config.mjs</h4>
							<p class="text-body">Add the following to your <code class="code-inline">astro.config.mjs</code>:</p>
							<Code code={`export default defineConfig({
  output: 'static',
  site: 'https://username.github.io/repositoryname/',
});`} lang="javascript" />
						</div>
						<div class="mb-6">
							<h4 class="heading-3">3. Create GitHub Actions Workflow</h4>
							<p class="text-body">Create <code class="code-inline">.github/workflows/deploy.yml</code>:</p>
							<Code code={`name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout your repository using git
      	uses: actions/checkout@v5
      - name: Install, build, and upload your site
      	uses: withastro/action@v5
        with:
          path: . # The root location of your Astro project
          node-version: 20 # Node version to use
          package-manager: npm # Package manager to use

  deploy:
    needs: build
    uses: actions/deploy-pages@v4`} lang="yaml" />
						</div>
						<div class="mb-6">
							<h4 class="heading-3">4. Deploy</h4>
							<p class="text-body">
								Push to the <code class="code-inline">main</code> branch and GitHub Actions will automatically build and deploy your site. You can also manually trigger deployments from the <strong>Actions</strong> tab.
							</p>
						</div>
					</div>
					<div id="tab-stripe" class="tab-content hidden">
						<h3 class="heading-2">
							<span class="text-blue">Stripe Setup</span>
						</h3>
						<p class="text-body-lg">
							Setup instructions for Stripe FIAT payments will be added here.
						</p>
					</div>
					<div id="tab-x402" class="tab-content hidden">
						<h3 class="heading-2">
							<span class="text-blue">x402 Setup</span>
						</h3>
						<p class="text-body-lg">
							Setup instructions for x402 cryptocurrency payments will be added here.
						</p>
					</div>
				</div>
			</section>
		</main>
		<script>
			(function() {
				const themeToggle = document.getElementById('theme-toggle');
				const html = document.documentElement;
				const savedTheme = localStorage.getItem('theme') || 'dark';
				html.setAttribute('data-theme', savedTheme);
				themeToggle?.addEventListener('click', () => {
					const currentTheme = html.getAttribute('data-theme');
					const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
					html.setAttribute('data-theme', newTheme);
					localStorage.setItem('theme', newTheme);
				});
			})();
			document.addEventListener('DOMContentLoaded', () => {
				const tabButtons = document.querySelectorAll('.tab-button');
				const tabContents = document.querySelectorAll('.tab-content');
				tabButtons.forEach(button => {
					button.addEventListener('click', () => {
						const targetTab = button.getAttribute('data-tab');
						tabButtons.forEach(btn => {
							btn.classList.remove('active');
						});
						tabContents.forEach(content => {
							content.classList.add('hidden');
							content.classList.remove('active');
						});
						button.classList.add('active');
						const tabId = `tab-${targetTab}`;
						const targetContent = document.getElementById(tabId);
						if (targetContent) {
							targetContent.classList.remove('hidden');
							targetContent.classList.add('active');
						}
					});
				});
			});
		</script>

		<Footer />
	</body>
</html>

